export interface LoginState {
    inProgress: boolean,
    success: boolean,
    error: any
}

export interface SignupState {
    inProgress: boolean,
    success: boolean,
    error: any
}

export interface LogoutState {
    inProgress: boolean,
    success: boolean,
    error: any
}